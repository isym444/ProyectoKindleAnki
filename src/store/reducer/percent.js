const initialState = {
    kenshuui: 0,
    hp2es: 0
}

 const percent = (state = initialState, { type, payload }) => {
    switch (type) {

   /*  case typeName:
        return { ...state, ...payload } */

    default:
        return state
    }
}
export default percent

